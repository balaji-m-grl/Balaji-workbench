git add .
git commit -m "msg"
git push
git pull
git status
git branch
git checkout branch
git checkout -b "branch name"
git checkout branchname = to change branch
git push origin frontend
git push origin backend
merge conflict - it happens one branch developer do one line and also another branch developer do same line and commit . but you write  that code in local repo then and make add command and add commit . it tells to pull. then you pull it cause merge conflict . it cause make decision which line you want
git stash - if i write one line but not commit and then decided to pull but gives aborting because already have that line in main branch .so it tell me to do commit . i dont need mean use git stash
git stash pop -mean it will restore
git branch -a   for cloud created branch also shown
git log
